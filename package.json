{
	"name": "@donutteam/binary-rw",
	"version": "1.0.0",
	"description": "A library for reading and writing binary files in TypeScript.",
	"main": "./build/index.js",
	"scripts": {
		"build": "npx rimraf build && npx tsc",
		"build:watch": "npx rimraf build && npx tsc --watch",
		"prepublishOnly": "npm run build"
	},
	"keywords": [
		"binary",
		"read",
		"write"
	],
	"author": "Donut Team",
	"license": "MIT",
	"dependencies": {
		"@types/utf8": "^3.0.3",
		"utf8": "^3.0.0"
	},
	"devDependencies": {
		"@donutteam/typescript-config": "^1.0.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.4.5"
	}
}
